import array as arr  # evitar usar, a ideia é ser eficiente mas provavelmente será melhor usar a biblioteca numpy
import numpy as np

arr.array('d', [1, 5])

numeros = np.array([1, 3, 5])
print(numeros)

print(numeros + 3)
